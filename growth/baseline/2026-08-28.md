# Nimbo growth baseline — 2026-08-28

Verdict: **usable as a reported/read-only starting point; not yet a fully reproducible export baseline**. The canonical payload is [2026-08-28.json](2026-08-28.json). Store overview values and release state were rechecked read-only on 2026-08-28, but raw exports and overview storefront filters were not captured.

## Reported store values

| Platform | Reported evidence | Reproducibility status |
| --- | --- | --- |
| App Store | 206 impressions; 5 product-page views; 5 first-time downloads; 1 redownload; 3 updates; 4.05% conversion; 0 ratings; 1 crash | Storefront, source/device breakdown, unique impressions, sessions, and raw export missing |
| Google Play | 779 impressions; 21 installations; 14 first launches; 11 MAU; 40.82% conversion; reported 1 rating/public 10+ badge | Storefront/surface definition, visitor/click denominator, source/device breakdown, and raw export missing; console ratings/vitals suppressed as insufficient data |

The supplied counts do not reproduce the reported conversion values:

- Apple `5 / 206 = 2.43%`, but this is not Apple's conversion formula because those inputs are first-time downloads and unqualified impressions. Apple's metric requires total downloads/pre-orders and unique device impressions.
- Google `21 / 779 = 2.70%`; the 40.82% source denominator was not supplied.
- Google first launches/installations are directionally `14 / 21 = 66.67%`, below the 80% target, but the populations and windows are not confirmed comparable.

These are definition gaps, not permission to replace the reported values with derived ones.

The mutable release recheck found iOS `1.0.1 (4)` Ready for Distribution with Apple Watch included; Android phone `1.0.2 (6)` active in 177 countries with four version-specific installations; and Wear `1.0.2 (1000007)` active in 177 countries since 2026-08-27 19:43 (`Asia/Tashkent`). Play showed no unpublished changes. These observations prove console state on that date, not device smoke or future availability.

## Public discovery baseline

The [first fixed snapshot](../data/public-rank/2026-08-28.json) completed every configured fetch:

- Apple UZ Top Free Weather: Nimbo absent from the 100 observed entries, recorded as `>100`.
- Apple public UZ search for `weather`: exact response position `81`; the other four configured queries did not contain Nimbo in their returned result sets.
- Google UZ Weather category: absent from the 30 observed unique packages in each of `uz-UZ`, `ru-UZ`, and `en-UZ`, recorded as `>30` for each.
- Google UZ searches: no configured query placed Nimbo in the observed 30- or 50-package result set on any fixed profile.
- Verified Top-10 streak: `0 / 7` complete days.

Response hashes, request URLs, fixed language parameters, top-ten items, observed counts, and target bounds are retained in the JSON. Public positions remain observational and can differ from signed-in/device results.

## Required next baseline evidence

The first weekly import must include UZ summary rows plus available cuts by acquisition source, device, and app version. Missing privacy-thresholded metrics remain unknown. In particular, collect unique impressions/visitors, correct conversion numerators, retention cohorts, DAU/MAU populations, rating averages, iOS stability, Android vitals, user loss, and policy state.

The [iOS crash gate](../quality/ios-crash-gate.md) remains blocked: App Store Connect shows exactly one crash on 2026-08-25 filtered to iOS `1.0.1`, but device detail is suppressed as insufficient data and no stack trace is exposed. The exact build-4 dSYM is available and UUID-matched to its binary, but it cannot be matched to the crash without the crash binary UUID/report.
